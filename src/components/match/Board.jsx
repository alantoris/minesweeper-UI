import React, { Fragment } from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    
  }));


const Board = (props) => {

    const classes = useStyles();

    return (
        <Fragment>Board</Fragment>
    )
}

export default Board;